/*
 * cuBlasGetPointerMode_mex.c
 *
 * Auxiliary code for mexFunction of cuBlasGetPointerMode
 *
 * C source code generated by m2c.
 * %#m2c options: codegenArgs=-args {CuBlasHandle}  enableInline=1
 *
 */

#ifdef MATLAB_MEX_FILE
#include "mex.h"
#else
#include "matrix.h"
#endif
/* Include the C file generated by codegen in lib mode */
#include "cuBlasGetPointerMode.h"
#include "m2c.c"
/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"

void cuBlasGetPointerMode_api(const mxArray ** prhs, const mxArray **plhs) {

    struct0_T            hdl;
    mxArray              *_sub_mx1;

    int32_T              *mode;
    int32_T              *errCode;
    boolean_T            *toplevel;

    /* Marshall in function inputs */

    if (!mxIsStruct(prhs[0]))
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputType",
            "Input argument hdl has incorrect data type. struct is expected.");
    if (mxGetNumberOfFields(prhs[0])!=3)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:InputStructWrongFields",
            "Input argument hdl has incorrect number of fields.");
    if (mxGetNumberOfElements(prhs[0]) != 1)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongSizeOfInputArg",
            "Argument hdl must contain 1 items.");

    _sub_mx1 = mxGetField(prhs[0], 0, "data");
    if (_sub_mx1==NULL)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputStruct",
            "Input argument hdl does not have the field data.");
    if (mxGetNumberOfElements(_sub_mx1) && mxGetClassID(_sub_mx1) != mxUINT8_CLASS)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputType",
            "Input argument hdl.data has incorrect data type. uint8 is expected.");
    *(void**)&hdl.data = mxCalloc(1, sizeof(emxArray__common));
    alias_mxArray_to_emxArray(_sub_mx1, (emxArray__common*)hdl.data, "hdl.data", 1);
    _sub_mx1 = mxGetField(prhs[0], 0, "type");
    if (_sub_mx1==NULL)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputStruct",
            "Input argument hdl does not have the field type.");
    if (mxGetNumberOfElements(_sub_mx1) && mxGetClassID(_sub_mx1) != mxCHAR_CLASS)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputType",
            "Input argument hdl.type has incorrect data type. char is expected.");
    *(void**)&hdl.type = mxCalloc(1, sizeof(emxArray__common));
    alias_mxArray_to_emxArray(_sub_mx1, (emxArray__common*)hdl.type, "hdl.type", 2);
    _sub_mx1 = mxGetField(prhs[0], 0, "nitems");
    if (_sub_mx1==NULL)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputStruct",
            "Input argument hdl does not have the field nitems.");
    if (mxGetNumberOfElements(_sub_mx1) && mxGetClassID(_sub_mx1) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongInputType",
            "Input argument hdl.nitems has incorrect data type. int32 is expected.");
    if (mxGetNumberOfElements(_sub_mx1) != 1)
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongSizeOfInputArg",
            "Argument hdl.nitems should be a scalar.");
    hdl.nitems = *(int32_T*)mxGetData(_sub_mx1);

    /* Preallocate output variables */
    {mwSize l_size[] = {1, 1};
    *(void **)&mode = prealloc_mxArray((mxArray**)&plhs[0], mxINT32_CLASS, 2, l_size); }
    {mwSize l_size[] = {1, 1};
    *(void **)&errCode = prealloc_mxArray((mxArray**)&plhs[1], mxINT32_CLASS, 2, l_size); }
    {mwSize l_size[] = {1, 1};
    *(void **)&toplevel = prealloc_mxArray((mxArray**)&plhs[2], mxLOGICAL_CLASS, 2, l_size); }

    /* Invoke the target function */
    cuBlasGetPointerMode_initialize();

    cuBlasGetPointerMode(&hdl, mode, errCode, toplevel);

    cuBlasGetPointerMode_terminate();

    /* Marshall out function outputs */
    /* Nothing to do for plhs[0] */
    /* Nothing to do for plhs[1] */
    /* Nothing to do for plhs[2] */

    /* Free temporary variables */
    free_emxArray((emxArray__common*)hdl.type); mxFree(hdl.type);
    free_emxArray((emxArray__common*)hdl.data); mxFree(hdl.data);

}

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[3];
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 1) {
        if (nlhs > 3)
            mexErrMsgIdAndTxt("cuBlasGetPointerMode:TooManyOutputArguments","Too many output arguments for entry-point cuBlasGetPointerMode.");
        /* Call the API function. */
        cuBlasGetPointerMode_api(prhs, (const mxArray**)outputs);
    }
    else
        mexErrMsgIdAndTxt("cuBlasGetPointerMode:WrongNumberOfInputs","Incorrect number of input variables for entry-point cuBlasGetPointerMode.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}